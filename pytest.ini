[pytest]
# Without asyncio_mode=auto, every `async def test_...` is SILENTLY SKIPPED
# with a warning you will not read. This is the single most common way people
# ship an async test suite that tests nothing.
asyncio_mode = auto
testpaths = tests
