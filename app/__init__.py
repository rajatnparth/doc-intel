"""doc-intel — a multi-tenant document intelligence API.

This file is empty on purpose. Its EXISTENCE is what makes `app/` a package,
so that `from app.config import settings` works. (Python 3.3+ technically
allows namespace packages without it, but an explicit __init__.py is what
every real project ships — it makes the package boundary intentional.)
"""
